#############################################Project##############################################
add_subdirectory(Core)
if(${PHYSICA_GUI})
    add_subdirectory(Gui)
endif()
add_subdirectory(Logger)    
if(${PHYSICA_PYTHON})
    add_subdirectory(Python)
endif()

if(${PHYSICA_GUI})
    add_executable(Physica Physica.cpp ${HEADERS})
    target_link_libraries(Physica PhysicaCore PhysicaGui Qt6::Core Qt6::Widgets ${HDF5_CXX_SHARED_LIBRARY} ${GPerfTools_LIBRARY})
    set_target_properties(Physica PROPERTIES AUTOMOC ON)
endif()
